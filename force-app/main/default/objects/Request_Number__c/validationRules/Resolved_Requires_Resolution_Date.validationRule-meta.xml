<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Resolved_Requires_Resolution_Date</fullName>
    <active>true</active>
    <description>Requires a Resolution Date when a service request is marked Resolved.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, &quot;Resolved&quot;),
    ISBLANK(Resolution_Date__c)
)</errorConditionFormula>
    <errorDisplayField>Resolution_Date__c</errorDisplayField>
    <errorMessage>A Resolution Date is required when the request is marked Resolved.</errorMessage>
</ValidationRule>
